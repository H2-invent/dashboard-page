services:
    app:
        build:
            context: .
        container_name: dashboard-app
        ports:
            - '8888:8000'
        volumes:
            - ./:/app
        env_file:
            - .env
